name: Run nf-raffle Pipeline

on:
  workflow_dispatch:
    inputs:
      email:
        description: "Email address for raffle entry"
        required: true
        default: "fake.name@fakeplace.com"
        type: string
      event:
        description: "Event name"
        required: false
        type: string
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install Nextflow
        uses: nf-core/setup-nextflow@6c2e22b4d901f0c42ca66c5069f8026df026d165 # v2
        with:
          version: "latest"

      - name: Run nf-raffle pipeline
        run: |
          nextflow run . \
            --email "${{ github.event.inputs.email || 'fake.name@fakeplace.com' }}" \
            ${{ github.event.inputs.event && format('--event {0}', github.event.inputs.event) || '' }} \
            --outdir results

      - name: Upload results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: pipeline-results
          path: results/
          retention-days: 7

      - name: Upload logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: nextflow-logs
          path: |
            .nextflow.log
            work/
          retention-days: 7
